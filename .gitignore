# ไม่เอา Virtual Environment
.venv/
venv/
__pycache__/

# ไม่เอาไฟล์ความปลอดภัย (สำคัญมาก)
*.pem
*.key

# ไม่เอาไฟล์ Database หรือไฟล์ชั่วคราวอื่นๆ
*.log
.DS_Store

# (ทางเลือก) ถ้าไฟล์ Model ใหญ่เกิน 100MB จะอัพไม่ผ่าน
# ถ้าไฟล์ .engine หรือ .onnx ใหญ่มาก ให้เอา # ด้านล่างออก
# *.engine
# *.onnx